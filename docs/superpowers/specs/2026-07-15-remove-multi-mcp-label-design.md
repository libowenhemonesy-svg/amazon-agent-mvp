# 移除 AI 选品多 MCP 数据源标签设计

## 目标

移除 AI 选品导航区中面向用户显示的“多 MCP 数据源”标签。

## 范围

- 删除 `app/static/index.html` 中该单一标签。
- 保持 AI 选品的数据源调用、错误提示、关键词采集和原始数据展示逻辑不变。
- 在前端静态测试中断言该文案不会重新出现。

## 验收

- AI 选品页面源码中不包含“多 MCP 数据源”。
- 现有 AI 选品前端测试继续通过。
