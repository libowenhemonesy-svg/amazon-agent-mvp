# 卖家精灵字段表格设计

## 目标

AI 选品仅以表格展示卖家精灵 MCP 返回的字段，并将字段列标题展示为中文。

## 范围

- 在前端维护卖家精灵字段名到中文标题的映射。
- 未映射字段以“其他字段（原字段名）”展示，避免遗漏真实 MCP 数据。
- 删除原始响应卡片、快照渲染与关键词列表中的原始数据查看入口。
- 保留完整字段表格和所有真实字段值。

## 验收

- 表格标题不直接显示已映射的英文字段名。
- 页面不再包含原始响应展示容器或“查看”入口。
- 前端静态测试覆盖上述条件。
